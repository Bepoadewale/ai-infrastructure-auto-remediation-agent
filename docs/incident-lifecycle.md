# Incident lifecycle

`DETECTED → INVESTIGATING → DIAGNOSING → PLAN_PROPOSED → APPROVAL_REQUIRED/APPROVED → REMEDIATING → VERIFYING → RESOLVED|ESCALATED`. Each transition is appended to the incident timeline with principal and timestamp.
