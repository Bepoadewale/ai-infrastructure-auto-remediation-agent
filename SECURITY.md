# Security

This local lab intentionally contains no Kubernetes credentials. Do not add kubeconfigs, secrets or cloud credentials. The design forbids arbitrary shell/kubectl execution and cluster-admin access.
